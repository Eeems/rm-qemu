#!/bin/bash
args=(
  -p ${RM_SSH_PORT:-22}
  -o StrictHostKeyChecking=no
)
if [[ "$RM_EXEC_ENABLED_PASSWORD" != "1" ]];then
  args+=(-o PasswordAuthentication=no)
fi
args+=(root@localhost)
if [ "$#" -ne 0 ]; then
  args+=(
    /bin/sh
    -lc
    "'$*'"
  )
fi
exec ssh ${args[@]}
