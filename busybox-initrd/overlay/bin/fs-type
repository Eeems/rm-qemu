#!/bin/sh
# shellcheck disable=SC3010,3014
if [ $# -ne 1 ] || [[ "$1" == "--help" ]] || [[ "$1" == "-h" ]]; then
  echo "Usage: fs-type <device or file>"
  exit 1
fi
/sbin/blkid -o full "$1" | xargs -n1 | grep -E '^TYPE=' | cut -d= -f2 | tail -n1
