#!/bin/sh
set -e
if [ "$#" -eq 0 ]; then
  echo "Usage: serial-client <path-to-socket>" >/dev/stderr
  exit 1
fi
socket="$1"
shift
if [ "$#" -ne 0 ]; then
  echo "$@" | nc -NU "$socket"
else
  exec nc -NU "$socket"
fi
