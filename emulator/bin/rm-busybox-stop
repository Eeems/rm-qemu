#!/bin/bash
set -e
# shellcheck disable=SC2016
echo 'kill -9 "$(pgrep -P 1 sh)"' | rm-busybox-exec 2>/dev/null || true
while lsof "${ROOTDIR}/artifact/busybox.img" 2>/dev/null | grep -q qemu-system-arm; do
  sleep 1
done
